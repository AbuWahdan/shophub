import '../core/theme/app_theme.dart';

class LightColor {
  const LightColor._();

  static const background = AppColors.background;
  static const titleTextColor = AppColors.textPrimary;
  static const subTitleTextColor = AppColors.textSecondary;

  static const skyBlue = AppColors.primary;
  static const lightBlue = AppColors.primaryLight;

  static const orange = AppColors.accent;
  static const red = AppColors.error;

  static const lightGrey = AppColors.border;
  static const grey = AppColors.textHint;
  static const darkgrey = AppColors.textSecondary;

  static const iconColor = AppColors.textHint;
  static const yellowColor = AppColors.warning;

  static const black = AppColors.textPrimary;
  static const lightblack = AppColors.textSecondary;
}
